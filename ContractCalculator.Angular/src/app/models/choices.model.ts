import { ContractChoices } from './contract-choices.model';
import { PhoneChoices } from './phone-choices.model';
import { ContactChoices } from './contact-choices.model';

export class Choices {
  contractChoices: ContractChoices;
  phoneChoices: PhoneChoices;
  dataUsage: number;
  contact: ContactChoices;
}
